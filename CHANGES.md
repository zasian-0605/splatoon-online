# v25 変更点

## 直した不具合（試し撃ち場に行けない原因）
1. 試し撃ち場が描画されない
   - 別ブロックから `setupFighterEconomy` / `regenPlayer` / `consumeInk` / `gearProfile` / `sfx` などが呼べず、毎フレーム ReferenceError で `render()` まで届かなかった → `window` に公開
   - `openOnlinePanel` / `onlineSocket` / `onlinePlayerId` も同様（オンラインの READY ボタンが動かなかった）
2. 街が作れない: three.js r128 に無い `THREE.CapsuleGeometry` を NPC に使っていた → プリミティブで代替。装飾が失敗しても街に入れるよう try/catch
3. 施設遷移中に街へ戻される: `leaveLobby()` が phase=1(ニュース) に戻し、クリック/Space でニュース終了処理が再実行されていた → phase 1.2 (遷移中) に変更、ニュース終了は1回だけ
4. 街のアバターの `squid` モデルを `false` で上書き → Shift(イカ化)で毎フレーム例外

## その他
- カスタマイズ画面のイベント二重登録（街への遷移が複数回走る）を修正、プレビュー用 WebGL コンテキストを破棄
- 試し撃ち後にメニューでカーソルが消えたままになる問題、オンライン画面/マップでマウスがロックされたままの問題
- インクのシミが Z 方向に長い楕円になる問題（キャンバスは正方形・ステージは 56x140）
- 街に戦闘ステージが重なって表示される問題
- 対戦開始時にインク/デカール/スポンジをリセット
- 描画ループの二重化を防止。フレーム内で例外が出ても画面が固まらない
- v17 の移動処理で無効化されていたスーパージャンプ / ジャンプ台 / ランチャー / スピナー解除を再接続
- 「P」で出ていた「準備中」トースト削除、Tailwind CDN(未使用・読み込みをブロック)削除
- 街: スマホで右スワイプ視点、キー押しっぱなし固着の防止

## server.js
- 公開ファイルを `index.html` のみに制限（以前は server.js 等も取得できた）、`/healthz` 追加
- WebSocket の ping（Render のアイドル切断対策）、メッセージサイズ制限
- **ダメージ同期を追加**（`hit` メッセージ）。以前はオンラインで誰にもダメージが入らなかった
- スポーン位置をステージに合わせ z=±38 に修正
